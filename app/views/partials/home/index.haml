/ .grid.wrap.block.index
/   %div{'ng:controller' => 'LocationController'}
/     %h1 Place.by
/     Укажите ваше расположение
/     %form{ name:'currentLocation', 'ng:submit' => 'getCoordinateAddress()' }
/       %input{ name: 'address', 'ng:model' => 'currentAddress' }
/     %button{ 'ng:show' => 'showCoordinateGeo', 'ng:click' => 'getCoordinateGeo()' } O_o
/     %button{ 'ng:click' => 'clicsds()' } O_o
/     Location: {{coordinate}}
/     = link_to 'rooms', '/rooms'
/     = link_to 'add room', '/rooms/new'

.wrap.block.index
  .grid
    .unit.w-1-1
      Выберите категорию
  .grid
    - Category.roots.each do |category|
      .unit.w-1-3
        .title= category.title
        - category.children.each do |sub_category|
          = link_to sub_category.title, category_path(sub_category.slug), class: 'subtitle'